projects.dir.name=projects
3rdparty.project.name=3rd-party
core.project.name=core
repository.project.name=repository
web-client.project.name=web-client
installer.project.name=installer

projects.dir=${basedir}/${projects.dir.name}
3rdparty.project.dir=${projects.dir}/${3rdparty.project.name}
core.project.dir=${projects.dir}/${core.project.name}
repository.project.dir=${projects.dir}/${repository.project.name}
web-client.project.dir=${projects.dir}/${web-client.project.name}
installer.project.dir=${projects.dir}/${installer.project.name}

project.build.file=project-build.xml
deploy.file=${web-client.project.dir}/build/dist/${web-client.project.name}.war

tomcat.dist.filename=${ant.project.name}-tomcat-${version.number}
jboss.dist.filename=${ant.project.name}-jboss-${version.number}
src.dist.filename=${ant.project.name}-src-${version.number}
src.nightly.dist.filename=${ant.project.name}-src-${date}

tomcat.distribution.dir=${basedir}/../tomcat-distribution
jboss.distribution.dir=${basedir}/../jboss-distribution