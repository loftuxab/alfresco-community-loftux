module.id=phpIntegration

project.dir=.
config.dir=${project.dir}/config
dist.dir=${project.dir}/build/dist
property.dir=${project.dir}/config/alfresco/module/${module.id}

file.jar=${module.id}.jar
package.file.jar=${dist.dir}/${file.jar}
