var endpoint = "support.alfresco.com";
model.endpoint = endpoint;
model.endpointUrl = url.context + "/proxy/" + endpoint + "";

//var connector = remote.connect(endpoint);
//var html = connector.call("/");
//model.html = html;
