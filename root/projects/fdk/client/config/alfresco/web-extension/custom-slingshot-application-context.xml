<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE beans PUBLIC '-//SPRING//DTD BEAN//EN' 'http://www.springframework.org/dtd/spring-beans.dtd'>

<beans>

   <!-- 
   This file is a temporary workaround, we need the fdk-context.xml file to be loaded
   last but there isn't a generic hook point currently, this file is a custom Share
   hook point, one will be needed for each app the FDK is dropped into.
    -->
   <import resource="classpath:alfresco/fdk-context.xml"/>

</beans>