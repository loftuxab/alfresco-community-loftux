## 
# This class path configuration file points the benchmark test at a Tomcat installation of the Alfresco repository.
# If you wish to benchmark Alfresco installed differently then change these classpath values.

# MySQL Driver jar (Change this if you using a different database)
${TOMCAT_HOME}/common/lib/mysql-connector-java-3.1.12-bin.jar

# Repository config files
${TOMCAT_HOME}/webapps/alfresco/WEB-INF/classes

# Repository jars
${TOMCAT_HOME}/webapps/alfresco/WEB-INF/lib/*.jar

# Classpath to extensions that might be installed
#${TOMCAT_HOME}/shared/classes
#${TOMCAT_HOME}/shared/lib/*.jar

# The benchmark jars and config
./lib/*.jar
./lib/japex/*.jar
./lib/jdsl/*.jar
./config
