<project name="benchmark-tests" default="build">

    <path id="path.common" path="${basedir}/../../common"/>
    <property name="dir.common" refid="path.common" />
    <import file="${dir.common}/common.xml" />

    <target name="package-war">
        <echo>
         This project can not produce a WAR file, the Remote API functionality is currently
         packaged within the Web Client WAR file.
      </echo>
    </target>

	<target name="jmeter-deploy" depends="compile-java" description="Deploys the unit tests to JMeter">
        <mkdir dir="${dir.dist}" />
        <jar jarfile="C:/jakarta-jmeter-2.1.1/lib/junit/${file.name.jar}" basedir="${dir.classes}" />
    </target>
	
</project>
