<alfresco-config>

   <plug-ins>
      <element-readers>
         <element-reader element-name="default-controls" class="org.alfresco.web.config.DefaultControlsElementReader"/>
         <element-reader element-name="constraint-handlers" class="org.alfresco.web.config.ConstraintHandlersElementReader"/>
         <element-reader element-name="form" class="org.alfresco.web.config.FormElementReader"/>
      </element-readers>
   </plug-ins>

   <config>
      <default-controls>
         <type name="d:text" template="controls/textfield.ftl">
            <control-param name="size">50</control-param>
         </type>
         <type name="d:long" template="controls/textfield.ftl">
            <control-param name="size">20</control-param>
         </type>
         <type name="d:boolean" template="controls/checkbox.ftl" />
      </default-controls>
      <constraint-handlers>
         <constraint type="REGEX" validation-handler="Alfresco.forms.validation.regexMatch" 
                     message="The name can not contain the character '{0}'"/>
      </constraint-handlers>
   </config>
   
   <!-- Default form configuration for the cm:content type -->
   <config evaluator="string-compare" condition="cm:content">
      <form>
         <field-visibility>
            <show id="cm:name" />
            <show id="cm:title" />
            <show id="cm:description" />
         </field-visibility>
         <appearance>
            <!-- NOTE: Currently the fields have to be repeated here, they shouldn't have to be -->
            <field id="cm:name"  >
               <constraint-message type="REGEX" message="The document name can not contain the character '{0}'" />
            </field>
            <field id="cm:title" />
            <field id="cm:description" />
         </appearance>
      </form>
   </config>
   
   <!-- Default form configuration for the cm:folder type -->
   <config evaluator="string-compare" condition="cm:folder">
      <form>
         <field-visibility>
            <show id="cm:name" />
            <show id="cm:title" />
            <hide id="cm:description" />
         </field-visibility>
         <appearance>
            <!-- NOTE: Currently the fields have to be repeated here, they shouldn't have to be -->
            <field id="cm:name" >
               <constraint-message type="REGEX" message="The space name can not contain the character '{0}'" />
            </field>
            <field id="cm:title" />
            <field id="cm:description" />
         </appearance>
      </form>
   </config>

</alfresco-config>