<project name="cifs-server" default="build">

   <path id="common.path" path="${basedir}/../.." />
   <property name="common.dir" refid="common.path" />
   <import file="${common.dir}/common.xml" />

   <!-- we only build a JAR file in this project -->
   <target name="package" depends="package-jar" />

</project>
