<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE beans PUBLIC '-//SPRING//DTD BEAN//EN' 'http://www.springframework.org/dtd/spring-beans.dtd'>
 
<beans>
	
   <!--
      These beans are not wrapped in their own transactions, as they do no work unless
      there is a transaction present.
      Also, the transactionManager depends on these.
   -->
   
   <bean id="integrityDaoService" class="org.alfresco.repo.integrity.db.hibernate.HibernateIntegrityDaoServiceImpl" >
      <property name="sessionFactory">
         <ref bean="sessionFactory" />
      </property>
   </bean>
   
   <bean id="integrityService" class="org.alfresco.repo.integrity.db.DbIntegrityServiceImpl" init-method="init">
      <property name="policyComponent">
         <ref bean="policyComponent"/>
      </property>
      <property name="dictionaryService">
         <ref bean="dictionaryService" />
      </property>
      <property name="nodeService">
         <ref bean="nodeService" />
      </property>
      <property name="integrityDaoService">
         <ref bean="integrityDaoService" />
      </property>
      <property name="enabled">
         <value>false</value>  <!-- on/off switch -->
      </property>
      <property name="traceOn">
         <value>false</value>  <!-- use only to trace problems -->
      </property>
      <property name="failOnViolation" >
         <value>true</value>  <!-- set to true to generate runtime exceptions upon violation -->
      </property>
      <property name="maxErrorsPerTransaction" >
         <value>5</value>  <!-- limit output (exception and log) to the first N violation messages -->
      </property>
      <property name="flushSize" >
         <value>100000</value>  <!-- number of nodes to process before clearing caches -->
      </property>
   </bean>
   
</beans>
