<project name="repository" default="build">

   <path id="common.path" path="${basedir}/../.." />
   <property name="common.dir" refid="common.path" />
   <import file="${common.dir}/common.xml" />

   <target name="compile-java" depends="common.compile-java">
      <antcall target="compile-jibx" />
      <copy todir="${classes.dir}">
         <fileset dir="${src.test-resources.dir}" />
      </copy>
   </target>

   <target name="compile-jibx" depends="init">
      <bind verbose="false" load="true" binding="${jibx.binding.file}">
         <classpathset dir="${classes.dir}"/>
      </bind>
   </target>

</project>
