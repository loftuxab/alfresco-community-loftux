<#escape x as jsonUtils.encodeJSONString(x)>
   {      
      "SAMLRequest" : "${SAMLRequest}",
      "Signature" : "${Signature}",
      "SigAlg" : "${SigAlg}",
      "KeyInfo" : "${KeyInfo}",
      "action" : "${action}"
   }
</#escape>

