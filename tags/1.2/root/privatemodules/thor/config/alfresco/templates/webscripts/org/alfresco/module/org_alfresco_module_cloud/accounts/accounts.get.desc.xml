<webscript>
   <shortname>GET accounts</shortname>
   <description><![CDATA[
   Gets list of filtered / paged accounts (which can also be optionally sorted).
   If the optional "sortBy" parameter is given, then the results may be sorted. Possible values are "typeId" (default ASCending), "typeId:ASC", "typeId:DESC".
   ]]>
   </description>
   <url>/internal/cloud/accounts?typeId={typeId}&amp;startIndex={startIndex?}&amp;pageSize={pageSize?}&amp;sortBy={sortBy?}</url>
   <format default="json">argument</format>
   <authentication>admin</authentication>
   <transaction allow="readonly">required</transaction>
   <lifecycle>internal</lifecycle>
</webscript>