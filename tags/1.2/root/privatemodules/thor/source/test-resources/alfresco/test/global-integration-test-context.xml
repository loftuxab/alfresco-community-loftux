<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
      http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd
      http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd">

    <!-- This file is a near-duplicate of a file with the same name in the 'repository' project.
         This duplicate should not need to exist and instead we should be able to reuse the file
         from repository.
         This duplication will be removed after the code convergence and build mavenisation.
         
         TODO Add JIRA number.  -->
    
    
    <!--
    These files commented out as they are not needed currently in Cloud - these folders & resources should
    be available as maven dependencies and reused as necessary. See above.
    
    <import resource="classpath:alfresco/test/integration/community/*-context.xml"/>
    <import resource="classpath:alfresco/test/integration/enterprise/*-context.xml"/>
    -->
    
    <import resource="classpath:alfresco/test/integration/cloud/*-context.xml"/>
</beans>
